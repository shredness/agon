# Rate limiting zones (http context — conf.d files are included inside http)
# 10 requests/minute per IP for auth + external data endpoints
limit_req_zone $binary_remote_addr zone=auth_limit:10m rate=10r/m;
limit_req_zone $binary_remote_addr zone=external_limit:10m rate=30r/m;
